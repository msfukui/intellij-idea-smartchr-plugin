<idea-plugin>
    <id>com.github.msfukui.smartchr</id>
    <name>SmartChr</name>
    <vendor email="support@github.com/msfukui" url="https://github.com/msfukui">msfukui</vendor>

    <description><![CDATA[
    <p><strong>SmartChr</strong> brings vim-smartchr functionality to IntelliJ IDEA. Improve your coding efficiency by cycling through predefined character patterns with repeated key presses.</p>
    
    <h3>🚀 Key Features:</h3>
    <ul>
        <li><strong>Smart Key Cycling:</strong> Transform single keys into multiple string patterns</li>
        <li><strong>File Type Awareness:</strong> Different behaviors for different programming languages</li>
        <li><strong>JSON Configuration:</strong> Easy-to-edit JSON settings file</li>
        <li><strong>Two Modes:</strong> LOOP (cycle back to start) and ONE_OF (stop at last)</li>
        <li><strong>Wide Language Support:</strong> Java, Kotlin, Python, JavaScript, TypeScript, and more</li>
    </ul>
    
    <h3>💡 Example Usage:</h3>
    <ul>
        <li>Press <code>=</code> repeatedly: <code>=</code> → <code> = </code> → <code> == </code> → <code>=</code> (cycles)</li>
        <li>Press <code>.</code> in Java/Kotlin: <code>.</code> → <code>-></code> → <code>.</code> (cycles)</li>
        <li>Press <code>+</code> repeatedly: <code>+</code> → <code> + </code> → <code> += </code> → <code>+</code> (cycles)</li>
    </ul>
    
    <h3>🛠️ Easy Setup:</h3>
    <p>After installation, go to <strong>Settings → Editor → SmartChr</strong> and click "Open Config File" to customize your key mappings with simple JSON configuration.</p>
    ]]></description>

    <change-notes><![CDATA[
    <h3>1.0.0</h3>
    <ul>
        <li><strong>Initial stable release</strong></li>
        <li>✨ Core key cycling functionality with LOOP and ONE_OF modes</li>
        <li>📁 File type specific configurations</li>
        <li>⚙️ JSON-based configuration system</li>
        <li>🎨 Intuitive settings UI with integrated file editor</li>
        <li>🧪 Comprehensive test coverage (38 tests, 100% pass rate)</li>
        <li>📖 Complete user documentation and examples</li>
        <li>🔧 Support for automatic config file creation</li>
        <li>🌐 Wide programming language support</li>
    </ul>
    ]]></change-notes>

    <!-- Plugin version -->
    <version>1.0.0</version>

    <!-- Compatible with IntelliJ IDEA 2023.1 and later -->
    <idea-version since-build="231" until-build="252.*"/>

    <!-- Plugin dependencies -->
    <depends>com.intellij.modules.platform</depends>

    <!-- Extension points defined by the plugin -->
    <extensions defaultExtensionNs="com.intellij">
        <!-- Settings Configurable -->
        <applicationConfigurable
                parentId="editor"
                instance="com.github.msfukui.smartchr.settings.SmartChrConfigurable"
                id="com.github.msfukui.smartchr.settings"
                displayName="SmartChr"/>
        
        <!-- Application Service for settings -->
        <applicationService
                serviceImplementation="com.github.msfukui.smartchr.settings.SmartChrSettings"/>
    </extensions>

    <!-- Actions -->
    <actions>
        <!-- SmartChr will be registered programmatically -->
    </actions>

    <!-- Application listeners -->
    <applicationListeners>
        <listener class="com.github.msfukui.smartchr.SmartChrStartupListener"
                  topic="com.intellij.ide.AppLifecycleListener"/>
    </applicationListeners>
</idea-plugin>